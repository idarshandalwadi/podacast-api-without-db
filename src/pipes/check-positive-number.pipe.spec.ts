import { CheckPositiveNumberPipe } from './check-positive-number.pipe';

describe('CheckPositiveNumberPipe', () => {
  it('should be defined', () => {
    expect(new CheckPositiveNumberPipe()).toBeDefined();
  });
});
